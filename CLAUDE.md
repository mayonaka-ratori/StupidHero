# StupidHero

スマホのブラウザで遊ぶゲームです。中身は`README.md`、コードの地図とコマンドは`docs/DEVELOP.md`にあります。

## 作業の始めと終わりにすること

人はスマホで開発のダッシュボードを見て、進み具合を追ったり質問に答えたりします。セッションごとに次のことをします。

始めるとき:

1. `docs/BOARD.md`を読む。「質問」に答えが書いてあれば、それに合わせて作業する
2. これからやる作業を「やること」の表に`doing`で書く(なければ足す)

作業の途中:

- 人に決めてほしいことが出たら、「質問」の表に足して、決まるまでほかの作業を進める。止まる作業は`wait`にして、「止まっている理由」に質問のIDを書く

終わるとき:

1. 「やること」の状態を直す(終わったら`done`)
2. `node tools/dashboard.mjs`でダッシュボードを作り直す。テストか型の確かめが落ちると終了コード1になるので、直してから次へ進む
3. 書き出した`dashboard/index.html`を、Artifactとして次のURLに公開し直す(`url`に渡す。新しいURLは作らない)
   https://claude.ai/artifact/QoqCezjV6V5XfcT1veYkjH

## 文の書き方

ドキュメント、コミットのメッセージ、コードのコメントは、平易な日本語で書きます。エムダッシュは使いません。`docs/`の文では、日本語の文の中に半角スペースを入れません。ゲームの中の文の決まりは`docs/DEVELOP.md`の「文章を書くときの決まり」にあります。
